package org.gfg.array;

import com.mkp.GFG;
import org.junit.Assert;
import org.junit.Test;

@GFG(date = "11-03-2019", algo = "array sum", url = "", impls = Basic.class)
public class BasicTest {
    @Test
    public void testSum() {
        Assert.assertEquals(5, 4 + 1);
    }
}
